<%= form_for :post ,:url => {:action => 'edit' , :id => params[:id]} do  |post| %>

  <p>
    <%= post.label :content, " Edit Your Content:" %>
    <%= post.text_area :content , :value => @post.content %>
  </p>

  <p class='buttons'>
    <%= post.submit "Go" %>
  </p>

<% end %>